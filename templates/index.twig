<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ titulo }}</title>
</head>
<body>
    <h1>{{ name }}'s Profile {{ emoji }}</h1>
    <hr>
    
    <h2>Red Social</h2>
    <p>{{ social_network }}: <a href="{{ social_link }}" target="_blank">{{ social_link }}</a></p>

    <h2>Educación</h2>
    <ul>
        {% for edu in education %}
        <li>
            <strong>{{ edu.degree }}</strong>
        </li>
        {% endfor %}
    </ul>

    <h2>Aprendiendo</h2>
    <ol>
        {% for learn in learning %}
        <li><strong>{{ learn.language }}</strong></li>
        {% endfor %}
    </ol>
</body>
</html>

